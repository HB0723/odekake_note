<% content_for :title, "おでかけを編集" %>

<h1 class="h3 mb-3">おでかけを編集</h1>

<div class="card border-0 shadow-sm">
  <div class="card-body">
    <%= render "form", outing: @outing, cancel_path: outing_path(@outing) %>
  </div>
</div>
